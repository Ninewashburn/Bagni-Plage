export const AUTH_HERO_STYLES = `
  .auth-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, transparent 35%, var(--bg) 100%);
  }
  .auth-hero-content {
    position: absolute;
    bottom: 60px;
    left: 28px;
    color: #fbf5e9;
  }
  .estate-label {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.875rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    opacity: 0.85;
  }
  .hero-title {
    font-family: var(--font-display);
    font-size: 2.75rem;
    line-height: 1;
    margin-top: 4px;
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.2);
  }
`;
